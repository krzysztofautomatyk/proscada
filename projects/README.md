# ProScada projects

The factory **Water Tank Dual-Pump** project is embedded in the Rust core
(`water_tank_project()`) and loaded automatically on startup.

Use **File · Export JSON** in the app to save a `.proscada.json` snapshot here.
